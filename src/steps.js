/*      ============ Basic Router setup ============
        1. npm react-router-dom
        2. add routerProvider and set router prop
        3. add a router using createBrowserRouter
        4. Create lot of components
        5. create Main layout with some common part and some one changing part base on the route
        6. set main layout at the root of the route
        7. set children routes
        8. set links on the header components
*/
